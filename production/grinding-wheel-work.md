# 砂轮工单

结构
--------------------------------------------------------------------------
### Schema

Column                              | Type      | Null | Note
------------------------------------|-----------|------|-------
`id`                                | int       | No   | 
`mix_id`                            | int       | No   | 
`status`                            | int       | No   | Lookup `grinding-wheel-work-status` 
`pressed_by`                        | int       | No   | 
`process`                           | int       | No   | 

状态：已创建(1)、已生效(2)、已作废(4)

操作
--------------------------------------------------------------------------
### 交付
`deliver-mix`. 仅存储压块员列
### 退回
`refuse-mix`. 混料员在交付环节回传在选错压块员的情况，通过此操作，可以让压块员将工单退回，以便混料员重新选择。

此操作就是交付操作的逆操作，将 `pressed_by` 重置为 null.

### 接收
`receive-mix`. 逻辑要点：

- Work 工序更新为”压块“；
- 新增 `grinding_wheel_work_press` 记录；

变更
--------------------------------------------------------------------------
- 2024-10-24 调整 新建逻辑，不再输入备选批次栏,质检只需要录入质量要求
